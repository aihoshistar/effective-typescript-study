# 📂 [Week 1] Item 11~15: 타입 설계와 중복 제거

---

## 🏷️ Metadata

학습일: 2025년 12월 22일

주차: #Week1

키워드: #TypeScript #잉여속성체크 #함수표현식 #DRY #타입설계

---

## 🚀 핵심 요약

> [!abstract] 이 섹션의 핵심 한 줄
>
> (이곳에 아이템 11~15를 아우르는 핵심 인사이트를 한 줄로 정리해 보세요.)

---

## ✍️ 주요 내용 정리

### Item 11. 잉여 속성 체크의 한계 인지하기

- 변수에 직접 타입을 지정하면 오류를 찾아내지만, 임시 변수에 타입을 지정하면 부분집합이므로 타입 체커에 통과된다.
    - 전자는 구조적 타입 시스템에서 발생할 수 있는 잉여 속성 체크가 수행된다.
    - 잉여 체크 검사가 할당 가능 검사와는 별도 과정이다. (Mental Model == 심성 모형)
- 객체 리터럴을 변수나 함수의 매게변수에 전달할 때 잉여 속성 체크가 수행된다.
- 의도와 다르게 작성된 코드를 찾으려 한다.
    - darkMode 라는 타입과 darkmode 라는 속성 오탈자를 찾으려 한다.
    - document 라는 객체와 new HTMLAnchorElement 에는 title 이라는 속성이 있어 정상이다.
    - '엄격한 객체 리터럴 체크' 를 통해 방지가 가능하다. :
- 잉여 속성 체크는 단언문(타입 케스팅)에서도 적용되지 않는다.
- 잉야 속성 체크를 원하지 않을 경우, 인덱스 시그니처(컨트릭트)를 사용해 추가 속성을 예상할 수 있다.
- 임시 상수를 도입하름오러 잉여 속성 체크의 문제를 해결한다.
    - 문맥 관점의 오류는 #Item26 에서 다룬다.
- 타입스크립트 타입 체커가 수행하는 '일반적인 구조적 할당 가능성 체크'와 역할이 다르다.
    - 잉여 속성 체크 vs 일반적인 구조적 할당 가능성 체크 ==> 차이를 구분할 줄 알아야 한다.
- 공통 속성 체크 : 잉여 속성 체크와 동일하게 오타를 잡는데 좋다. (구체적 예제는 #Item18)

### Item 12. 함수 표현식에 타입 적용하기

```ts
function rollDice1(sides: number): number {..} // 함수 선언문 Function Declaration (또는 Function Statement)
const rollDice2 = function(sides: number): number {..} // 함수 표현식 (Function Expression)
const rollDice3 = (sides: number): number => {..} // 화살표 함수 (Arrow Function)
```

- 함수 문장 (statement) :
- 함수 표현식 (expression) :
- 함수 타입 선언의 장점
    - 불필요한 코드의 반복을 줄인다.
- 시그니처가 일치하는 다른 함수가 있을 때 함수 표현식에 타입을 적용할 수 있다.
    - 디자인패턴 (래퍼 패턴, 데코레이터 패턴, 프록시 패턴 등)을 적용할 수 있다.
    - fetch 는 일반적으로 Error Handling Decorator 인터셉터로 따로 구현을 하긴 한다.
    - Error Handling Decorator 을 위한 목적으로라도 인터셉터를 따로 만드는게 일반적인 것 같다
    - 관심사 분리가능
    - 함수 전체의 타입 선언을 적용해야한다.
- 타입을 계속 만들다기보단 이미 존재하는 타입을 찾아보도록 해야한다.
- 라이브러리를 직접 만든다면 공통 콜백 타입을 제공해야 한다.
- 다른 함수의 시그니처(컨트릭트)를 참고하려면 typeof fn 을 사용하면 된다.

```ts
// 내장 함수인 fetch의 타입을 그대로 가져온다.
// T: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>
type FetchFn = typeof fetch;

const monitoredFetch: FetchFn = async (input, init) => {
  const start = Date.now();
  
  try {
    const response = await fetch(input, init);
    const duration = Date.now() - start;
    console.log(`소요 시간: ${duration}ms`);
    return response;
  } catch (e) {
    console.error('에러:', e);
    throw e;
  }
};
```

- 함수 시그니처를 복사할 때 typeof를 사용하면, 원본 함수의 인터페이스를 그대로 계승하면서 로직만 살짝 바꾸는 래퍼(Wrapper)나 프록시(Proxy) 를 만들 때 타입 안전성을 유지할 수 있다.
    - 떄문에 외부 라이브러리를 사용할땐 패터나 프록시 패턴 등을 이용하면 유지보수에 좋다.  

### Item 13. 타입과 인터페이스의 차이점 알기

- 공통점
    - 대부분의 일반적인 상황에서는 서로 대체 가능하다
    - TypeScript는 구조적 타이핑(Structural Typing) 이라서, 모양이 같으면 type로 정의하던 interface로 정의하던 서로 대입되는 경우가 많다.
- type
    - 한 번 선언한 type은 다시 열어서 필드를 추가할 수 없다. (같은 이름으로 재선언 불가)
    - 고급 타입 문법(mapped type, conditional type, template literal type 등)으로 “타입을 변환/생성”하는 작업에 강하다.
- interface
    - interface는 객체 타입의 시그니쳐(컨트렉트)를 표현하는 선언이다.
    - interface는 열려 있어서 같은 이름으로 여러 번 선언하면 병합(declaration merging) 될 수 있다.
    - extends로 확장하는 형태가 가독성이 좋다.

차이점

- 표현 가능한 범위: type은 모든 타입, interface는 객체 형태이다.
    - type은 원시/유니온/튜플 등 어떤 타입에도 이름을 붙일 수 있다.
    - interface는 객체 shape에 집중한다. (원시 타입 별칭 불가)

```ts
type UserId = string; // 원시 타입 별칭
type Result = "ok" | "error"; // 유니온 타입

interface IUserId /* = string */ {
  // 이런 식으로 "string 별칭"은 만들 수 없음
  // 원시 타입 별칭 용도로는 부적합 (객체 shape 중심)
}
```

- 확장 방법: `interface extends` vs `type`의 교차(`&`)

```ts
// type 확장(교차 타입 &)
type AnimalT = {
  name: string;
};
type BearT = AnimalT & {
  honey: boolean;
};

// interface 확장
interface Animal {
  name: string;
}
interface Bear extends Animal {
  honey: boolean;
}

```

- 열려있음 (Re-open / Declaration Merging)
    - interface는 같은 이름을 여러 번 선언하면 자동으로 병합된다.
    - type은 같은 이름을 재선언하면 에러가 발생한다.
    - 라이브러리 타입을 프로젝트 상황에 맞게 확장해야 할 때, interface의 병합이 쓰인다.

```ts
// type: 재선언 불가
type ConfigT = { host: string };
type ConfigT = { port: number }; // Error: Duplicate identifier 'ConfigT'

// interface: 선언 병합 (필드 추가) 가능
interface Config {
  host: string;
}
interface Config {
  port: number;
}
const c: Config = { host: "localhost", port: 3000 };
```

- 클래스(class)와의 궁합: implements / 다형성관점
    - 일반적으로 클래스가 시그니처(컨트릭트)를 구현하는 의미로는 interface 가 자연스럽다. (다형성)
    - type도 객체 타입이면 implements가 가능하지만, 유니온 타입 같은 단일 shape가 아닌 타입은 구현할 수 없다.

```ts
// interface도 implements 가능하다.
interface Flyable { fly(): void; }
class Bird implements Flyable { fly() {} }

// type(객체)도 implements 가능하다.
type HasName = { name: string };
class Person implements HasName {
  name = "kim";
}

// type은 implements 불가능하다 (유니온)
type Id = { id: number } | { id: string };
class User implements Id { id = 1 } // Error
```

- 고급 타입 기능: mapped / conditional / template literal 등

고급 타입 기능은 interface로 하기 어렵다고 정리했다.

```ts
// mapped type + template literal type로 getter 자동 생성
type Getters<T> = {
  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K];
};

type User = {
  name: string;
  age: number;
};

type UserGetters = Getters<User>;
// 결과:
// {
//   getName: () => string;
//   getAge: () => number;
// }
```

- 튜플(tuple): type은 정석이며, interface는 우회 가능하지만 한계가 있다.
    - 튜플은 type으로 자연스럽게 정의한다.
    - interface로 비슷하게 흉내는 가능하지만, 배열 특성상 길이/인덱스 정밀도가 떨어질 수 있다.

```ts
// tuple은 type이 직관적이다.
type TeamMember = [name: string, role: string, age: number];
const m: TeamMember = ["Alice", "Dev", 28];
console.log(m[3]) // Error (길이 3 고정)

// interface로 우회(배열 확장) 가능하지만 튜플처럼 고정되지 않는다.
interface TeamMemberLike extends Array<string | number> {
  0: string;
  1: string;
  2: number;
}

const x: TeamMemberLike = ["Bob", "Dev", 30];
console.log(x[3]); // 타입 레벨에서는 허용되지만 값은 undefined 이다.
```

---

- conflict 처리: interface는 컴파일 타임에 막고, type 교차는 합치다가 이상해질 수 있다.
    - interface 확장 시 같은 키의 타입이 다르면 에러가 발생한다.
    - type 교차(`&`)는 합쳐지지만 결과가 never가 되는 등 예상치 못한 결과가 나올 수 있다.

```ts
// interface: 충돌은 에러
interface Person {
  name: string;
}
interface Staff extends Person {
  name: number; // Error
}

// type 교차: 충돌이 병합되며 결과가 never가 될 수 있음
type PersonT = { name: string };
type StaffT = PersonT & { name: number };

const s: StaffT = {
  name: "kim" // Error: string은 never에 할당 불가 (name이 사실상 never가 됨)
};
```

- interface가 더 자연스러운 경우
    - 객체의 시그니처(컨트릭트)를 표현하고 싶다. (도메인 모델, DTO, API 응답 객체 등)
    - 라이브러리/공개 API 타입처럼 확장/병합할 가능성이 있다.
    - extends 기반의 계층/확장이 많은 구조 (가독성 + 성능 이점 가능)

- type이 꼭 필요한 경우
    - 원시 타입 별칭: `type UserId = string;`
    - 유니온/디스크리미네이티드 유니온: `A | B`
    - 튜플: `[string, number]`
    - mapped/conditional/template literal 등 타입 연산이 필요한 경우

### Item 14. 타입 연산과 제네릭 사용으로 반복 줄이기

- 타입을 이용하여 반복되는 코드를 최대한 줄인다. (DRY)

```ts
type BaseEntity = { id: number; createdAt: string; updatedAt: string; };
type User = BaseEntity & { name: string; };
type Post = BaseEntity & { title: string; };

type ApiResponse<T> = { success: boolean; data: T; };

type UserResponse = ApiResponse<User>;
type PostResponse = ApiResponse<Post>;

-- 
interface Person { name: stirng };
interface PersonWithBirthDate extends Person { birth: Date };
```

- 디자인패턴을 잘 활용하여 반복되는 코드도 줄인다. (주객전도느낌)

```ts
function withErrorHandling<TArgs extends any[], TResult>(
  fn: (...args: TArgs) => Promise<TResult>
) {
  return async (...args: TArgs): Promise<TResult> => {
    try {
      return await fn(...args);
    } catch (e) {
      throw e;
    }
  };
}
const safeGetUser = withErrorHandling(getUser);
```

- 제너릭 타입은 타입을 위한 함수와 같다. 제너릭 타입을 사용하여 타입들 간에 매핑을 한다.

```ts
// 하나의 공통 틀을 선언
interface ApiResponse<T> {
  data: T;
  status: number;
}

type User = { name: string; age: number };s
const userRes: ApiResponse<User> = {
  data: { name: "Gemini", age: 2 },
  status: 200
};
```

- 타입 유틸을 사용하여 반복되는 코드를 줄인다. (<https://www.typescriptlang.org/ko/docs/handbook/utility-types.html>)

```ts
interface User {
  id: number;
  name: string;
  age: number;
}

type UserForSignUp = Omit<User, 'id'>;
type UserForUpdate = Partial<User>;
```

.. 등 반복 줄이는 방법이 너무 많다.

### Item 15. 동적 데이터에 인덱스 시그니처 사용하

---

## 💻 코드 예시

**❌ 지양해야 할 코드**

TypeScript

```
// 이곳에 타입 단언이나 객체 래퍼 타입 사용 등 좋지 않은 예시를 작성하세요.

```

**✅ 지향해야 할 코드**

TypeScript

```
// 이곳에 타입 선언이나 집합론적 관점이 잘 드러난 안전한 코드를 작성하세요.

```
